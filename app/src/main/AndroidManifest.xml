<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a.dietplanning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SplashActivity">

            <!-- android:theme="@style/AppTheme.Launcher1"> &lt;!&ndash; 解决启动前白屏现象 &ndash;&gt; -->
            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".question.BMIActivity" />
        <activity android:name=".question.CoefActivity" />
        <activity android:name=".question.InfoActivity" />
        <activity android:name=".question.TargetActivity" />
        <activity android:name=".activity.SearchFoodDetailActivity" />

        <service
            android:name=".service.RegisterService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".service.FoodService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.DietPlaningActivity" />
        <activity android:name=".activity.SearchFoodActivity" />
        <activity android:name=".activity.SetActivity" />
        <activity android:name=".activity.BodyDataActivity" />

        <service
            android:name=".service.SearchFoodService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".activity.DietSortActivity" />
        <activity android:name=".activity.DietRecordActivity"></activity>
    </application>

</manifest>